[build]
  publish = "exampleSite/public"
  command = "cd exampleSite && hugo --minify --gc --themesDir ../.."
  
[build.environment]
  HUGO_VERSION = "0.76.5"
  HUGO_THEME = "repo"
  HUGO_BASEURL = "/"